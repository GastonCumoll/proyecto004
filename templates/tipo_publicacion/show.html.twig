{% extends 'base1.html.twig' %}

{% block title %}TipoPublicacion{% endblock %}
{% block stylesheets %}
{{ parent() }}
    {{ encore_entry_link_tags('crudCss') }}
{% endblock  %}
{% block body %}
<h1 class="font-weight-bold text-center">Tipo de publicacion</h1>
<div class="container">
    <div class="mt-3 p-3 border border-secondary rounded bg-light">

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipo_publicacion.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tipo_publicacion.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tipo_publicacion_index') }}">
        <button type="button" class="btn btn-info"> Back to list <i class="fas fa-arrow-left"></i> </button>  
    </a>

    <a href="{{ path('tipo_publicacion_edit', {'id': tipo_publicacion.id}) }}">
        <button type="button" class="btn btn-success"> Edit <i class="fas fa-edit"></i></button> 
        <br><br>
    </a>
    {{ include('tipo_publicacion/_delete_form.html.twig') }}
</div>
</div>
{% endblock %}
