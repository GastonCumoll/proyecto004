<form method="post" action="{{ path('suscripcion_delete', {'id': suscripcion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ suscripcion.id) }}">
    <button class="btn btn-danger">Delete <i class="fas fa-trash-alt"></i></button>
</form>
